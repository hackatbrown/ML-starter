# Sample file where random code might be written. These methods can be arbitrarily
# as complex or as simple as you would like. We have provided you with
# a method for sample use. 

def isOdd(x : int) -> bool:
    return x % 2 == 1
